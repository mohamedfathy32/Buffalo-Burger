import { createBrowserRouter } from "react-router-dom";
import ProductCard from "../components/ProductCard";

const router = createBrowserRouter([{
    path: '/', element: <ProductCard />,children:[
        {index:true, element: <}
    ]
}])